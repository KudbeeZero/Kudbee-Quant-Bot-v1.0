requests>=2.31,<3
pandas>=2.0,<3
numpy>=1.24,<3
pyarrow>=14.0,<21
# Backend API (serves the website's Live Signals page)
fastapi>=0.110,<1
uvicorn>=0.27,<1
pydantic>=2.0,<3
# Required by starlette/fastapi TestClient (tests/test_api.py, test_alert_endpoint.py)
httpx>=0.27,<1
# Host CPU/mem/disk for the dashboard System panel (/api/metrics). The endpoint
# degrades gracefully if absent, but this is needed for the live dashboard's
# metrics to render on Render.
psutil>=5.9,<7
# Meta-labeling: secondary classifier that learns which primary signals to take
scikit-learn>=1.4,<2
# Top-100 crypto universe config (config/crypto_universe.yaml) + Kestra flow validation
PyYAML>=6.0,<7
# AI chart review (dashboard-only, default-OFF via ENABLE_AI_CHART_REVIEW).
# openai: server-side vision call; python-multipart: required by FastAPI to parse
# the multipart/form-data image upload (POST /api/chart-review).
openai>=1.30,<2
python-multipart>=0.0.9
