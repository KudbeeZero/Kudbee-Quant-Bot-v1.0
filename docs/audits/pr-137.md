# Audit — PR #137 (`claude/weekly-trades-status-z8thda`)

**Date:** 2026-07-05
**PR:** [#137](https://github.com/KudbeeZero/Kudbee-Quant-Bot-v1.0/pull/137) — "docs+research: weekly status,
rejected TP/stop tighten, rejected stop-to-TP1 (MEMORY §81/§82)"
**State at audit time:** OPEN, draft, CI green (2× `test` job + Cloudflare Pages, all `success`).
**Base/head SHA (per GitHub):** `6d6d7e083f80`..`4835816555366`
**Verdict: PASS** (independent subagent, fresh eyes; did not trust the PR body or MEMORY claims —
verified against the diff and by running code).

## Context
This PR was **not mentioned anywhere in `docs/HANDOFF.md`** prior to this audit — the baton had
been reconciled for the 2026-07-02 streaming session and never picked up PR #137 (opened
2026-07-03). Discovered via `mcp__github__list_pull_requests(state=open)`, not via the baton.

## Diff sanity
GitHub's reported base SHA (`6d6d7e0`) is **not an ancestor of head** — it's `main` having moved
ahead via bot `paper-trade: update journal` commits after the branch point, so a naive
`base..head` diff shows unrelated `data/heartbeat.json` / `data/notify_state.json` noise. The
auditor found the true merge-base (`e3f97b3f8`) and confirmed the real diff is exactly the 6 files
GitHub's file list shows: `docs/HANDOFF.md`, `docs/MEMORY.md` (+§81, +§82),
`kudbee_quant/backtest/bracket.py`, `kudbee_quant/backtest/resolver.py`, `kudbee_quant/cli.py`,
`tests/test_resolver_exits.py`. No workflow/config/live-execution files touched.

## Findings (all confirmed real, in scope, not overstated)
- **`stop_to_tp1` kwarg is genuinely default-off and byte-identical when omitted.**
  `resolver.py`: `cur_stop = tp1 if stop_to_tp1 else entry` — with the default `False`, this is
  exactly the prior `cur_stop = entry` behavior. Threaded the same way through `bracket.py` and
  `cli.py`'s `tp-backtest` subcommand.
- **Isolated to the research CLI, not the live/paper path.** Repo-wide grep for `stop_to_tp1`
  hits only `resolver.py`, `bracket.py`, `cli.py`. Zero hits in `kudbee_quant/paper/`,
  `kudbee_quant/journal/`, `kudbee_quant/execution/`; no `kudbee_quant/live/` directory exists.
- **The 3 new tests are non-trivial.** `tests/test_resolver_exits.py:134-183` uses concrete
  synthetic bar paths and asserts specific R-multiples (1.125 vs 1.5, plus a trade-off case where
  `stop_to_tp1` exits earlier) — genuinely exercises the breakeven-vs-TP1-price distinction.
- **The `tp-backtest` footgun claim is real.** `cli.py`: `--interval` defaults to `"1d"`,
  `--oos-frac` defaults to `0.0` — matches §81's claim exactly.
- **Mechanism verified by running the actual CLI** (via an isolated worktree at head): the
  `--stop-to-tp1` flag works, `--interval 1h --oos-frac 0.3` runs correctly, and the tight-exit /
  stop-to-tp1 variants come back negative vs. baseline in this environment's data run too —
  same direction as §81/§82's tables (exact historical figures aren't expected to reproduce
  byte-for-byte since market data differs by fetch window).
- **No live config/Telegram/workflow files touched** — the "nothing deployed" narrative holds.

## Tests
- Base (`e3f97b3f8`, true merge-base) via worktree: **737 passed, 0 failed.**
- Head (`4835816`) via worktree: **740 passed, 0 failed** (+3 = the new tests).
- The PR's own claim of 2 pre-existing failures
  (`test_score_trade_ride_beats_bankhalf_on_clean_winner`,
  `test_run_shadow_scores_via_injected_fetch`) did **not** reproduce in this sandbox — both pass at
  base and head alike here. Not a diff defect either way (base and head are equally green in this
  environment), but flagged as an environment-dependent detail in the PR's own test-count claim
  rather than confirmed exactly as stated.

## Scope creep / security
None. Every changed file is plausibly part of "weekly status + tested-and-rejected tighten +
tested-and-rejected stop-to-tp1." No network/SSRF/secrets surface touched.

## Bottom line
No corrective action needed. The diff holds up under independent verification: the new
`stop_to_tp1` knob is genuinely additive and default-off, isolated to research tooling, backed by
real tests, and the two rejected-hypothesis writeups (§81, §82) are honest, non-overstated,
correctly re-run on the bot's actual timeframe with an OOS holdout. Gate applied: PR merged as PASS
(see `docs/HANDOFF.md`).
